# 贡献指南 (Contributing Guide)

感谢你对 Mua 的关注！我们非常欢迎任何形式的贡献，无论是修复 bug、增加新功能，还是改进文档。

## 🛠️ 开发环境搭建

Mua 是基于 Tauri 2.0 开发的，你需要安装以下环境：

1.  **Node.js**: >= 20.x
2.  **pnpm**: >= 9.x
3.  **Rust**: >= 1.77.x
4.  **Tauri 依赖**: 请参考 [Tauri 官方文档](https://tauri.app/v2/guides/getting-started/prerequisites) 安装各平台的编译依赖。

### 步骤

1.  **Fork 并克隆仓库**

    ```bash
    git clone https://github.com/your-username/Mua.git
    cd Mua
    ```

2.  **安装前端依赖**

    ```bash
    pnpm install
    ```

3.  **启动开发模式**

    ```bash
    pnpm tauri:dev
    ```

## 📜 代码规范

### Git 提交规范

我们遵循标准的提交信息规范：

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档变更
- `style`: 代码格式（不影响代码运行的变动）
- `refactor`: 重构（既不是新增功能也不是修改 bug 的代码变动）
- `perf`: 性能优化
- `test`: 增加测试
- `chore`: 构建过程或辅助工具的变动

### 代码风格

- **前端**: 使用 Prettier 进行格式化。
- **后端**: 使用 `cargo fmt` 进行格式化。

## 🚀 贡献流程

1.  创建一个新的分支 (`git checkout -b feature/your-feature`)。
2.  提交你的修改 (`git commit -m 'feat: add some feature'`)。
3.  推送到远程分支 (`git push origin feature/your-feature`)。
4.  在 GitHub 上发起 Pull Request。

## 💬 交流

如果有任何问题或建议，欢迎在 GitHub Issues 中提出。
