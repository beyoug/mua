export {
    currentTheme,
    themes,
    colorMode,
    colorModes,
    particlesEnabled,
    systemPrefersDark,
    effectiveColorMode
} from './service';

export type {
    ThemeId,
    ColorMode,
    Theme
} from '$lib/types/theme';
